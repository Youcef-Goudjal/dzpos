import 'package:flutter/material.dart';

class UserNotActive extends StatelessWidget {
  const UserNotActive({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
